name: Publish

on:
  push:
    tags:
      - 'v*.*.*'
      - '!v0.3.0'

permissions:
  contents: read
  id-token: write

concurrency:
  group: publish
  cancel-in-progress: false

jobs:
  publish:
    name: Verify and publish package
    runs-on: ubuntu-latest
    environment: release
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - run: npm ci
      - name: Check tag and select npm distribution tag
        id: release
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          node scripts/verify-release.mjs
          echo "npm_tag=$(node scripts/npm-dist-tag.mjs "$RELEASE_TAG")" >> "$GITHUB_OUTPUT"
      - name: Run the complete publication gate
        run: npm run prepublishOnly
      - name: Publish with npm trusted publishing
        run: npm publish --access public --ignore-scripts --tag "${{ steps.release.outputs.npm_tag }}"
